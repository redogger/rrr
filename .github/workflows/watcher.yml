name: DULMS Watcher 24-7 (v9.0)

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      duration_min:
        description: 'Run duration in minutes'
        required: false
        default: '50'

concurrency:
  group: dulms-watcher-relay
  cancel-in-progress: false

permissions:
  contents: write
  actions: write

jobs:
  watch:
    runs-on: ubuntu-latest
    timeout-minutes: 65

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ── Restore state from cache ──
      - name: Restore state (cache)
        uses: actions/cache/restore@v4
        with:
          path: |
            .dulms-state.json
            .dulms-session.json
            .dulms-session-meta.json
            .dulms-audit.log
          key: dulms-state-v9-${{ github.run_id }}
          restore-keys: |
            dulms-state-v9-
            dulms-state-v8-
            dulms-state-v7-
            dulms-session-

      # ── Fallback: from artifact ──
      - name: Restore state (artifact fallback)
        if: hashFiles('.dulms-state.json') == ''
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: dulms-state-v9
          path: .

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Install Chromium
        run: npx playwright install chromium --with-deps

      # ── Run ──
      - name: Run watcher
        env:
          DULMS_USERNAME: ${{ secrets.DULMS_USERNAME }}
          DULMS_PASSWORD: ${{ secrets.DULMS_PASSWORD }}
          TG_TOKEN:       ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID:     ${{ secrets.TG_CHAT_ID }}
          DURATION_MIN:   ${{ github.event.inputs.duration_min || '50' }}
          NODE_OPTIONS:   '--max-old-space-size=1024'
        run: node watch.js

      # ── Persist ──
      - name: Save state (cache)
        if: always()
        uses: actions/cache/save@v4
        with:
          path: |
            .dulms-state.json
            .dulms-session.json
            .dulms-session-meta.json
            .dulms-audit.log
          key: dulms-state-v9-${{ github.run_id }}

      - name: Save state (artifact backup)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dulms-state-v9
          path: |
            .dulms-state.json
            .dulms-session.json
            .dulms-session-meta.json
          retention-days: 14
          overwrite: true
          if-no-files-found: ignore

      # ── Relay ──
      - name: Relay next run
        if: always()
        run: |
          sleep 5
          gh workflow run watcher.yml --ref "${GITHUB_REF_NAME}" \
            || gh workflow run watcher.yml --ref main \
            || true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
